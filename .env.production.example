# Opcional: variables en runtime para el servidor SSR (express en server/server.mjs).
# PM2 puede leer un .env si lo cargas con dotenv o con `pm2 start ... --env production`.
# La URL del API y dominios públicos van en src/environments/environment.prod.ts (requieren nuevo build).
PORT=4201
